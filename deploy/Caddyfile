# Автоматический HTTPS без собственного домена: sslip.io резолвит имя в IP,
# Let's Encrypt выдаёт на него настоящий сертификат.
79-137-248-221.sslip.io {
	encode zstd gzip
	reverse_proxy 127.0.0.1:8000
}
